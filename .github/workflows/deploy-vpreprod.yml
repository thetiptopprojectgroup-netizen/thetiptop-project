# -----------------------------------------------------------------------------
# CD — Branche vpreprod — push `vpreprod` + attente CI — Client verte (backend déjà exigé avant), ou manuel.
# Secret multiligne : VPREPROD_ENV_FILE (contenu de infra/deploy/env/vpreprod.env)
# Monitoring (partagé VPS) : MONITORING_GRAFANA_ADMIN_PASSWORD ou MONITORING_ENV_FILE / _B64
# Logs (Kibana) : LOGGING_ELASTIC_PASSWORD ou LOGGING_ENV_FILE / _B64
# -----------------------------------------------------------------------------
name: CD / vpreprod

on:
  push:
    branches: [vpreprod]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: deploy-vpreprod
  cancel-in-progress: false

env:
  HARBOR_PROJECT: vpreprod
  VITE_API_URL: https://vpreprod.dsp5-archi-o22a-15m-g3.fr/api
  SITE_URL: https://vpreprod.dsp5-archi-o22a-15m-g3.fr

jobs:
  gate:
    name: "0 · CI — Client verte sur ce commit (ou manuel)"
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.gate.outputs.sha }}
      ok: ${{ steps.gate.outputs.ok }}
    steps:
      - name: Vérifier / attendre la CI
        id: gate
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const ciName = 'CI — Client';
            const target = 'vpreprod';
            if (context.eventName === 'workflow_dispatch') {
              core.setOutput('sha', context.sha);
              core.setOutput('ok', 'true');
              core.info('Déploiement manuel : pas d’attente CI.');
              return;
            }
            const sha = context.sha;
            const branch = context.ref.replace(/^refs\/heads\//, '');
            if (branch !== target) {
              core.setOutput('sha', '');
              core.setOutput('ok', 'false');
              return;
            }
            core.info(
              `Attente « ${ciName} » (push) verte pour ${sha.slice(0, 7)}…`
            );
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const maxAttempts = 90;
            const delayMs = 10000;
            const eventOk = (e) => e === 'push';
            for (let i = 0; i < maxAttempts; i++) {
              const { data } = await github.rest.actions.listWorkflowRunsForRepo({
                owner,
                repo,
                head_sha: sha,
                per_page: 50,
              });
              const subset = (data.workflow_runs || []).filter(
                (w) => w.name === ciName && w.head_branch === target && eventOk(w.event)
              );
              if (subset.length === 0) {
                core.info(`Run « ${ciName} » push pas encore visible (tentative ${i + 1}/${maxAttempts})`);
                await new Promise((r) => setTimeout(r, delayMs));
                continue;
              }
              const latest = subset.sort(
                (a, b) => new Date(b.created_at) - new Date(a.created_at)
              )[0];
              if (latest.status !== 'completed') {
                core.info(
                  `${ciName} : ${latest.status} (#${latest.id}) — tentative ${i + 1}/${maxAttempts}`
                );
                await new Promise((r) => setTimeout(r, delayMs));
                continue;
              }
              if (latest.conclusion === 'success') {
                core.setOutput('sha', sha);
                core.setOutput('ok', 'true');
                core.info(`${ciName} validée : run #${latest.id}`);
                return;
              }
              core.setOutput('sha', sha);
              core.setOutput('ok', 'false');
              core.setFailed(
                `${ciName} en échec (${latest.conclusion}) — déploiement annulé.`
              );
              return;
            }
            core.setOutput('sha', '');
            core.setOutput('ok', 'false');
            core.setFailed(`Timeout : « ${ciName} » (push) pas verte à temps — déploiement annulé.`);

  harbor-project:
    name: "1 · Harbor — projet vpreprod"
    needs: [gate]
    if: needs.gate.outputs.ok == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      sha: ${{ needs.gate.outputs.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.gate.outputs.sha }}

      - name: Créer le projet s'il n'existe pas
        env:
          HARBOR_URL: https://${{ secrets.HARBOR_REGISTRY_BASE }}
          HARBOR_ADMIN_USER: ${{ secrets.HARBOR_ADMIN_USER }}
          HARBOR_ADMIN_PASSWORD: ${{ secrets.HARBOR_ADMIN_PASSWORD }}
          HARBOR_PROJECT_NAME: ${{ env.HARBOR_PROJECT }}
        run: bash infra/scripts/ensure-harbor-project.sh

  build-and-push:
    name: "2 · Registry — build & push images"
    needs: [harbor-project]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      sha: ${{ needs.harbor-project.outputs.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.harbor-project.outputs.sha }}

      - name: Docker login (robot)
        run: |
          REGISTRY="${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}"
          LOGIN_SERVER="${REGISTRY%%/*}"
          echo "${{ secrets.HARBOR_PASSWORD }}" | docker login "${LOGIN_SERVER}" \
            -u "${{ secrets.HARBOR_USERNAME }}" --password-stdin

      - name: Build & push API
        run: |
          REGISTRY="${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}"
          SHA="${{ needs.harbor-project.outputs.sha }}"
          docker build -t "${REGISTRY}/api:${SHA}" -f server/Dockerfile server
          docker push "${REGISTRY}/api:${SHA}"

      - name: Build & push client
        env:
          VITE_GOOGLE_CLIENT_ID: ${{ vars.VITE_GOOGLE_CLIENT_ID || secrets.VITE_GOOGLE_CLIENT_ID }}
          VITE_GA_MEASUREMENT_ID: ${{ vars.VITE_GA_MEASUREMENT_ID || secrets.VITE_GA_MEASUREMENT_ID }}
          SHA: ${{ needs.harbor-project.outputs.sha }}
        run: |
          REGISTRY="${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}"
          LASTMOD_DATE="$(git log -1 --format=%cs)"
          docker build \
            --target production \
            --build-arg VITE_API_URL="${{ env.VITE_API_URL }}" \
            --build-arg SITE_URL="${{ env.SITE_URL }}" \
            --build-arg LASTMOD_DATE="${LASTMOD_DATE}" \
            --build-arg VITE_GOOGLE_CLIENT_ID="${VITE_GOOGLE_CLIENT_ID}" \
            --build-arg VITE_GA_MEASUREMENT_ID="${VITE_GA_MEASUREMENT_ID}" \
            -t "${REGISTRY}/client:${SHA}" \
            -f client/Dockerfile client
          docker push "${REGISTRY}/client:${SHA}"

  deploy-vps:
    name: "3 · VPS — rsync & docker compose"
    needs: [build-and-push]
    runs-on: ubuntu-latest
    timeout-minutes: 25
    outputs:
      sha: ${{ needs.build-and-push.outputs.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.build-and-push.outputs.sha }}

      - name: Écrire vpreprod.env sur le VPS + sync + compose
        env:
          SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_KEY }}
          VPREPROD_ENV_FILE: ${{ secrets.VPREPROD_ENV_FILE || secrets.VPREPROD_ENV }}
          VPREPROD_ENV_FILE_B64: ${{ secrets.VPREPROD_ENV_FILE_B64 }}
          MONITORING_ENV_FILE: ${{ secrets.MONITORING_ENV_FILE }}
          MONITORING_ENV_FILE_B64: ${{ secrets.MONITORING_ENV_FILE_B64 }}
          MONITORING_GRAFANA_ADMIN_PASSWORD: ${{ secrets.MONITORING_GRAFANA_ADMIN_PASSWORD }}
          LOGGING_ENV_FILE: ${{ secrets.LOGGING_ENV_FILE }}
          LOGGING_ENV_FILE_B64: ${{ secrets.LOGGING_ENV_FILE_B64 }}
          LOGGING_ELASTIC_PASSWORD: ${{ secrets.LOGGING_ELASTIC_PASSWORD }}
          LOGGING_KIBANA_HOST: ${{ secrets.LOGGING_KIBANA_HOST }}
          LOGGING_ADMIN_PASSWORD: ${{ secrets.LOGGING_ADMIN_PASSWORD }}
          LOGGING_KIBANA_SYSTEM_PASSWORD: ${{ secrets.LOGGING_KIBANA_SYSTEM_PASSWORD }}
          HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
          HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
          HARBOR_REGISTRY_HOST_RAW: ${{ secrets.HARBOR_REGISTRY_BASE }}
        run: |
          set -euo pipefail
          if [ -n "${VPREPROD_ENV_FILE_B64:-}" ]; then
            CONTENT="$(printf '%s' "$VPREPROD_ENV_FILE_B64" | base64 -d)"
          elif [ -n "${VPREPROD_ENV_FILE:-}" ]; then
            CONTENT="$VPREPROD_ENV_FILE"
          else
            echo "::error::Secret VPREPROD_ENV_FILE (ou VPREPROD_ENV / VPREPROD_ENV_FILE_B64) manquant. Collez le contenu de vpreprod.env dans un secret de dépôt (voir deploy-vdev.yml pour le même principe)."
            exit 1
          fi
          CONTENT="$(printf '%s' "$CONTENT" | tr -d '\r')"
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts
          REMOTE="${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_HOST }}"
          SSH_BASE=(ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -o TCPKeepAlive=yes)
          "${SSH_BASE[@]}" "$REMOTE" "mkdir -p /opt/thetiptop/app/infra/deploy/env"
          printf '%s\n' "$CONTENT" | "${SSH_BASE[@]}" "$REMOTE" "cat > /opt/thetiptop/app/infra/deploy/env/vpreprod.env"
          "${SSH_BASE[@]}" "$REMOTE" "chmod 600 /opt/thetiptop/app/infra/deploy/env/vpreprod.env"
          source infra/deploy/ci-write-remote-monitoring-env.sh
          source infra/deploy/ci-write-remote-logging-env.sh
          RSYNC_SSH='ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -o TCPKeepAlive=yes'
          rsync -avz --delete \
            --exclude '.git' \
            --exclude 'node_modules' \
            --exclude 'client/node_modules' \
            --exclude 'server/node_modules' \
            --exclude 'infra/deploy/env/*.env' \
            --exclude 'infra/monitoring/.env' \
            --exclude 'infra/logging/.env' \
            -e "$RSYNC_SSH" \
            ./ "${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_HOST }}:/opt/thetiptop/app/"
          HARBOR_REGISTRY_HOST="${HARBOR_REGISTRY_HOST_RAW#https://}"
          HARBOR_REGISTRY_HOST="${HARBOR_REGISTRY_HOST#http://}"
          HARBOR_REGISTRY_HOST="${HARBOR_REGISTRY_HOST%%/*}"
          printf '%s\n' "$HARBOR_PASSWORD" | "${SSH_BASE[@]}" "$REMOTE" \
            "docker login \"$HARBOR_REGISTRY_HOST\" -u \"$HARBOR_USERNAME\" --password-stdin"
          ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -o TCPKeepAlive=yes \
            "${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_HOST }}" \
            "set -euo pipefail
             cd /opt/thetiptop/app
             export REGISTRY='${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}'
             export IMAGE_TAG='${{ needs.build-and-push.outputs.sha }}'
             docker compose -f infra/deploy/docker-compose.stack.yml \
               --env-file infra/deploy/env/vpreprod.env \
               up -d --remove-orphans
             bash infra/deploy/seed-app-users.sh /opt/thetiptop/app infra/deploy/env/vpreprod.env
             bash infra/deploy/apply-traefik-minio-from-app.sh /opt/thetiptop/app
             bash infra/deploy/apply-monitoring-from-app.sh /opt/thetiptop/app
             bash infra/deploy/apply-logging-from-app.sh /opt/thetiptop/app"

      - name: Restic — crons (Ansible depuis le runner)
        run: bash infra/deploy/run-ansible-restic.sh
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_SSH_USER: ${{ secrets.VPS_SSH_USER }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}

      - name: Rétention dumps Mongo (5 fichiers max par env)
        run: bash infra/deploy/run-remote-prune-mongodb-dumps.sh
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_SSH_USER: ${{ secrets.VPS_SSH_USER }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}

  open-promotion-pr:
    name: "4 · PR promotion (brouillon)"
    needs: [deploy-vps]
    if: success() && github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: read
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Vérifier CI + CD puis ouvrir PR [Promotion] (brouillon)
        uses: actions/github-script@v8
        env:
          PROMOTION_MODE: vpreprod
          COMMIT_FULL: ${{ needs.deploy-vps.outputs.sha }}
          CD_RUN_ID: ${{ github.run_id }}
          CD_HTML_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        with:
          script: |
            const path = require('path');
            const main = require(path.join(process.env.GITHUB_WORKSPACE, '.github/scripts/open-promotion-pr-after-cd.cjs'));
            await main({ github, core, context });
