# -----------------------------------------------------------------------------
# CD — Branche vpreprod
# Secret multiligne : VPREPROD_ENV_FILE (contenu de infra/deploy/env/vpreprod.env)
# -----------------------------------------------------------------------------
name: CD / vpreprod

on:
  push:
    branches: [vpreprod]

permissions:
  contents: read

concurrency:
  group: deploy-vpreprod
  cancel-in-progress: true

env:
  HARBOR_PROJECT: vpreprod
  VITE_API_URL: https://vpreprod.dsp5-archi-o22a-15m-g3.fr/api
  SITE_URL: https://vpreprod.dsp5-archi-o22a-15m-g3.fr

jobs:
  harbor-project:
    name: "1 · Harbor — projet vpreprod"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Créer le projet s'il n'existe pas
        env:
          HARBOR_URL: https://${{ secrets.HARBOR_REGISTRY_BASE }}
          HARBOR_ADMIN_USER: ${{ secrets.HARBOR_ADMIN_USER }}
          HARBOR_ADMIN_PASSWORD: ${{ secrets.HARBOR_ADMIN_PASSWORD }}
          HARBOR_PROJECT_NAME: ${{ env.HARBOR_PROJECT }}
        run: bash infra/scripts/ensure-harbor-project.sh

  build-and-push:
    name: "2 · Registry — build & push images"
    needs: [harbor-project]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Docker login (robot)
        run: |
          REGISTRY="${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}"
          LOGIN_SERVER="${REGISTRY%%/*}"
          echo "${{ secrets.HARBOR_PASSWORD }}" | docker login "${LOGIN_SERVER}" \
            -u "${{ secrets.HARBOR_USERNAME }}" --password-stdin

      - name: Build & push API
        run: |
          REGISTRY="${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}"
          docker build -t "${REGISTRY}/api:${{ github.sha }}" -f server/Dockerfile server
          docker push "${REGISTRY}/api:${{ github.sha }}"

      - name: Build & push client
        env:
          VITE_GOOGLE_CLIENT_ID: ${{ vars.VITE_GOOGLE_CLIENT_ID || secrets.VITE_GOOGLE_CLIENT_ID }}
        run: |
          REGISTRY="${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}"
          LASTMOD_DATE="$(git log -1 --format=%cs)"
          docker build \
            --target production \
            --build-arg VITE_API_URL="${{ env.VITE_API_URL }}" \
            --build-arg SITE_URL="${{ env.SITE_URL }}" \
            --build-arg LASTMOD_DATE="${LASTMOD_DATE}" \
            --build-arg VITE_GOOGLE_CLIENT_ID="${VITE_GOOGLE_CLIENT_ID}" \
            -t "${REGISTRY}/client:${{ github.sha }}" \
            -f client/Dockerfile client
          docker push "${REGISTRY}/client:${{ github.sha }}"

  deploy-vps:
    name: "3 · VPS — rsync & docker compose"
    needs: [build-and-push]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Écrire vpreprod.env sur le VPS + sync + compose
        env:
          SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_KEY }}
          VPREPROD_ENV_FILE: ${{ secrets.VPREPROD_ENV_FILE || secrets.VPREPROD_ENV }}
          VPREPROD_ENV_FILE_B64: ${{ secrets.VPREPROD_ENV_FILE_B64 }}
          HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
          HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
          HARBOR_REGISTRY_HOST_RAW: ${{ secrets.HARBOR_REGISTRY_BASE }}
        run: |
          set -euo pipefail
          if [ -n "${VPREPROD_ENV_FILE_B64:-}" ]; then
            CONTENT="$(printf '%s' "$VPREPROD_ENV_FILE_B64" | base64 -d)"
          elif [ -n "${VPREPROD_ENV_FILE:-}" ]; then
            CONTENT="$VPREPROD_ENV_FILE"
          else
            echo "::error::Secret VPREPROD_ENV_FILE (ou VPREPROD_ENV / VPREPROD_ENV_FILE_B64) manquant. Collez le contenu de vpreprod.env dans un secret de dépôt (voir deploy-vdev.yml pour le même principe)."
            exit 1
          fi
          CONTENT="$(printf '%s' "$CONTENT" | tr -d '\r')"
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts
          REMOTE="${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_HOST }}"
          SSH_BASE=(ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes)
          "${SSH_BASE[@]}" "$REMOTE" "mkdir -p /opt/thetiptop/app/infra/deploy/env"
          printf '%s\n' "$CONTENT" | "${SSH_BASE[@]}" "$REMOTE" "cat > /opt/thetiptop/app/infra/deploy/env/vpreprod.env"
          "${SSH_BASE[@]}" "$REMOTE" "chmod 600 /opt/thetiptop/app/infra/deploy/env/vpreprod.env"
          RSYNC_SSH='ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes'
          rsync -avz --delete \
            --exclude '.git' \
            --exclude 'node_modules' \
            --exclude 'client/node_modules' \
            --exclude 'server/node_modules' \
            --exclude 'infra/deploy/env/*.env' \
            -e "$RSYNC_SSH" \
            ./ "${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_HOST }}:/opt/thetiptop/app/"
          HARBOR_REGISTRY_HOST="${HARBOR_REGISTRY_HOST_RAW#https://}"
          HARBOR_REGISTRY_HOST="${HARBOR_REGISTRY_HOST#http://}"
          HARBOR_REGISTRY_HOST="${HARBOR_REGISTRY_HOST%%/*}"
          printf '%s\n' "$HARBOR_PASSWORD" | "${SSH_BASE[@]}" "$REMOTE" \
            "docker login \"$HARBOR_REGISTRY_HOST\" -u \"$HARBOR_USERNAME\" --password-stdin"
          ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes \
            "${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_HOST }}" \
            "set -euo pipefail
             cd /opt/thetiptop/app
             export REGISTRY='${{ secrets.HARBOR_REGISTRY_BASE }}/${{ env.HARBOR_PROJECT }}'
             export IMAGE_TAG='${{ github.sha }}'
             docker compose -f infra/deploy/docker-compose.stack.yml \
               --env-file infra/deploy/env/vpreprod.env \
               up -d --remove-orphans
             bash infra/deploy/seed-app-users.sh /opt/thetiptop/app infra/deploy/env/vpreprod.env"

  # ---------------------------------------------------------------------------
  # Stage 4 — Ouvre une PR vpreprod → vprod (brouillon) après CD vert.
  # Aucun merge automatique : validation sur GitHub comme pour vdev → vpreprod.
  # ---------------------------------------------------------------------------
  promotion-pr:
    name: "4 · PR promotion → vprod"
    needs: [deploy-vps]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Ouvrir PR vpreprod → vprod si absente
        uses: actions/github-script@v8
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          COMMIT_SHORT: ${{ github.sha }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const base = 'vprod';
            const headBranch = 'vpreprod';
            const runUrl = process.env.RUN_URL;
            const sha = (process.env.COMMIT_SHORT || '').slice(0, 7);

            try {
              await github.rest.repos.getBranch({ owner, repo, branch: base });
            } catch (e) {
              if (e.status !== 404) throw e;
              const { data: repoInfo } = await github.rest.repos.get({ owner, repo });
              const def = repoInfo.default_branch;
              if (def === headBranch) {
                core.setFailed(
                  `La branche « ${base} » n’existe pas. Crée-la une fois sur GitHub ` +
                    `(Code → New branch « ${base} » depuis une branche adaptée), puis relance ce job ou repousse sur « ${headBranch} ».`
                );
                return;
              }
              const { data: refData } = await github.rest.git.getRef({
                owner, repo,
                ref: `heads/${def}`,
              });
              await github.rest.git.createRef({
                owner, repo,
                ref: `refs/heads/${base}`,
                sha: refData.object.sha,
              });
              core.info(`Branche « ${base} » créée (même pointeur que « ${def} »).`);
            }

            const { data: open } = await github.rest.pulls.list({
              owner, repo, state: 'open', base, per_page: 100,
            });
            const exists = open.some(
              (p) => p.head.ref === headBranch && p.base.ref === base
            );
            if (exists) {
              const pr = open.find((p) => p.head.ref === headBranch);
              core.info(`PR déjà ouverte (#${pr.number}).`);
              return;
            }

            const body = [
              '## Promotion vers la production (vprod)',
              '',
              'Cette PR est en **brouillon** : cliquez **Ready for review** quand vous validez, puis **Merge**.',
              '',
              '1. Vérifier sur **vpreprod** si besoin.',
              '2. **Merge** → lance le CD **production**.',
              '',
              '---',
              '',
              '**Ouverture automatique de la PR** seulement (après déploiement vpreprod réussi) — **pas de fusion automatique**.',
              '',
              `- Run : [CD / vpreprod](${runUrl})`,
              `- Commit : \`${sha}\``,
            ].join('\n');

            try {
              const { data: created } = await github.rest.pulls.create({
                owner, repo,
                head: headBranch,
                base,
                title: '[Promotion] vpreprod → vprod',
                body,
                draft: true,
              });
              core.info(`PR créée : #${created.number}`);
            } catch (err) {
              if (err.status !== 422) throw err;
              const raw = err.response?.data || {};
              const msg = JSON.stringify(raw).toLowerCase();
              const errors = Array.isArray(raw.errors) ? raw.errors : [];
              const errText = errors.map((e) => (e.message || e.code || '')).join(' ').toLowerCase();
              const combined = `${msg} ${errText}`;
              if (combined.includes('no commits') || combined.includes('nothing to compare')) {
                core.info(
                  'Aucune PR à ouvrir : pas de différence entre vpreprod et vprod (souvent le même commit). ' +
                    'C’est attendu après un merge de promotion ou si vprod a été alignée sur vpreprod.'
                );
                return;
              }
              if (combined.includes('already exists') || combined.includes('pull request already')) {
                core.info('Une PR existe déjà pour ce couple de branches (réponse API 422).');
                return;
              }
              core.error(`GitHub API 422 : ${JSON.stringify(raw)}`);
              throw err;
            }
