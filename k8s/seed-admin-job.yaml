# Même technique qu'en dev : création admin/employés uniquement (rapide).
# On attend la fin de ce job avant de continuer le déploiement.
apiVersion: batch/v1
kind: Job
metadata:
  name: seed-admin
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app: seed-admin
spec:
  ttlSecondsAfterFinished: 600
  backoffLimit: 4
  activeDeadlineSeconds: 300
  template:
    metadata:
      labels:
        app: seed-admin
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: harbor-secret
      initContainers:
        - name: wait-mongodb
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "En attente du port MongoDB 27017..."
              until nc -z mongodb 27017 2>/dev/null; do
                echo "  MongoDB pas encore joignable, nouvel essai dans 3s..."
                sleep 3
              done
              echo "MongoDB joignable, lancement du seed."
      containers:
        - name: seed-admin
          image: BACKEND_IMAGE_PLACEHOLDER
          imagePullPolicy: Always
          command: ["npm", "run", "seed:admin"]
          env:
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: backend-secret
                  key: mongodb-uri
            - name: ADMIN_EMAIL
              value: "ADMIN_EMAIL_PLACEHOLDER"
            - name: ADMIN_PASSWORD
              value: "ADMIN_PASSWORD_PLACEHOLDER"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
